# inject-app-loading

该插件用于在 Vite 应用 HTML 中注入首屏加载动画，改善管理端资源加载期间的用户体验。

## 职责

- 在应用挂载前展示 loading 结构或样式。
- 配合 `VITE_INJECT_APP_LOADING` 开关启用或关闭。
- 只处理首屏加载态，不负责业务页面的异步 loading。

## 维护约束

- 插件应保持框架无关，不依赖具体业务接口。
- loading 样式应足够轻量，避免拖慢 HTML 首屏解析。
- 如果管理端品牌视觉变化，可在此调整默认加载动画。


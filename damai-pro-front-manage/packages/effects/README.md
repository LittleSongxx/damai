# packages/effects

`effects` 存放与状态、副作用、路由、偏好设置和组件库存在轻耦合的共享逻辑。

## 适用内容

- Pinia 状态与异步副作用。
- 用户偏好、浏览器缓存、本地存储。
- 路由跳转、菜单联动、布局状态。
- 与特定 UI 组件库有关的组合逻辑。

## 维护约束

- 纯工具函数优先放入 `packages/utils`。
- 底层框架能力优先放入 `packages/@core`。
- 强业务逻辑不要放入 effects，避免共享包膨胀。


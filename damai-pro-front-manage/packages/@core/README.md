# packages/@core

`@core` 是后台管理前端的核心基础能力集合，来源于 Vben 体系中的底层 SDK、基础组件与工具封装。

## 职责

- 提供框架级通用能力。
- 承载基础 UI、组合式函数、偏好设置、图标、请求和布局等底层能力。
- 保持与具体 `damai-pro` 业务解耦。

## 维护约束

- 不要在此目录写业务逻辑。
- 不要依赖具体应用包。
- 可复用、稳定、跨应用的能力才适合放入该目录。


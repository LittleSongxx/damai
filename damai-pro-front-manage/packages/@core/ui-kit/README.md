# @core/ui-kit

`@core/ui-kit` 管理公共 UI 组件，以及对不同 UI 组件库的基础封装。

## 职责

- 抽象框架级 UI 能力。
- 封装多 UI 库可复用组件。
- 为上层应用提供稳定的 UI 基础设施。

## 维护约束

- 组件应保持业务无关。
- 不直接调用 `damai-pro` 后端接口。
- 与具体业务页面强绑定的组件应放在应用或业务包中。


# web-ele locales

该目录用于 `apps/web-ele` 的应用级国际化扩展。

## 职责

- 维护管理端页面自身的语言包。
- 扩展 dayjs、Element Plus 等第三方库的多语言切换。
- 承接应用级文案，不放通用框架包逻辑。

## 维护约束

- 新增页面文案时优先放在应用对应语言文件中。
- 多应用共享文案应下沉到 workspace 共享包，而不是复制到多个 app。
- 与后端枚举或字典相关的文案应保持和接口返回值一致。


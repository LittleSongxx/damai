# Damai 项目经历

## 项目经历

**大麦智能票务助手与高并发交易平台｜AI 应用开发 / 后端开发**

面向演出票务场景，构建集智能客服、辅助购票、知识问答、自然语言问数和智能运维于一体的 AI 应用平台。项目将 **LLM Agent** 接入真实票务交易链路，支持用户完成咨询、搜索、选票和确认下单，并为管理员提供知识治理、数据分析与故障诊断能力，重点解决 AI 应用落地中的可控执行、可信回答和稳定迭代问题。

## 技术栈

Java、Spring Boot、Spring Cloud、Spring AI、MySQL、Redis / Redisson、RabbitMQ、Elasticsearch、Qdrant、ShardingSphere、Prometheus、OpenTelemetry、RAG、NL2SQL

## 核心亮点

1. **票务 Agent 购票闭环：** 基于 **LLM Agent + Tool Calling + Slot Filling** 设计多轮购票流程，用状态机补齐城市、票价 / 票档、张数、购票人等关键槽位，再调用演出搜索、详情查询和购票预览工具；下单前必须用户确认，避免 AI 直接执行高风险交易。

2. **AI 下单交易一致性：** 将 AI 购票接入后端交易底座，用 Redis 缓存和 Lua 原子扣减承接高频库存操作，并结合锁、订单幂等、预留超时释放和 Saga 补偿思路，保障高并发场景下的库存与订单一致性。

3. **RAG 知识问答优化：** 构建 **Hybrid RAG** 链路，融合 FAQ 直答、Query Rewrite、向量检索、BM25 和重排策略，处理退票、实名制、入场规则等复杂咨询；长文档采用父子块检索和上下文窗口补全，提升答案依据完整性，证据不足时主动拒答或转人工。

4. **安全可控的自然语言问数：** 面向管理员实现 **NL2SQL** 能力，通过 Schema Linking 定位相关表和字段，再结合 SQL AST 校验、白名单、只读执行和敏感信息脱敏，限制大模型生成 SQL 的执行边界；对低置信问题主动澄清，并缓存稳定查询结果，提升问数效率。

5. **面向业务链路的智能运维：** 面向订单、库存、支付等核心链路建设 **AIOps** 诊断能力，根据服务名、订单号、预留单号或 Trace 信息聚合多源可观测信号，生成故障证据包、置信度和处理建议；证据不足时保留人工复核入口，避免 AI 给出过度确定的排障结论。

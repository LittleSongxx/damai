# 简历项目内容（两个后端项目）

## 项目一：大麦 — 分布式票务交易与 AI 智能助手平台

**大麦 — 分布式票务交易与 AI 智能助手平台**　　　　**核心开发者**　　　　**2025.10 - 至今**

- **项目简介：** 基于 **Spring Cloud Alibaba + Spring AI** 构建的分布式票务交易平台，覆盖节目检索、选座购票、异步下单与支付退款；以业务系统为事实源扩展 AI 智能助手，支持引导购票、知识问答、智能客服与运维诊断。
- **技术栈：** Java 17、Spring Cloud Alibaba、Nacos、Gateway、OpenFeign、Sentinel、Seata、ShardingSphere、MyBatis-Plus、MySQL、Redis/Redisson、RabbitMQ、Elasticsearch、Prometheus、Spring AI、Qdrant、MCP、SSE、Vue 3
- **职责与技术实现：**
  1. **选座抢票：** 基于 **Redis Lua** 完成余票校验、座位状态迁移与缓存预占，使用 **本地锁按票档粒度** 控制并发抢座冲突，以 **Redisson 分布式锁** 保障订单状态变更的互斥安全；相较早期下单链路，前置抢票吞吐大幅提升。
  2. **异步一致性：** 以 **RabbitMQ 异步建单** 削峰写库；MQ 消费端用 **Seata AT** 统筹订单落库与节目库扣减确认，配合消费幂等、超时关单与定时对账保障最终一致。
  3. **分片扩容：** 基于 **ShardingSphere 基因法** 对订单核心表按用户/订单维度复合分片（2库4表），并结合 Hint 路由实现增量式 **平滑数据迁移**。
  4. **AI 购票：** 基于 **Spring AI Tool Calling** 与对话状态机实现意图识别、槽位填充和购票预览；下单前注入 **幂等购票请求**，用户确认后调用订单服务。
  5. **RAG 客服：** 构建 **Dense + Sparse + HyDE → Evidence Gate 过滤 → RRF 融合 → Rerank → 后处理链** 的混合 RAG 问答，支持 **SSE 流式输出**；接入情感识别与人工升级工单。
  6. **运维韧性：** 以 **MCP** 封装日志、指标和 NL2SQL（**AST 只读校验**：仅允许 SELECT、表名白名单、禁分片表名、禁 SELECT *），结合诊断链定位异常并自修复重试；通过 **Sentinel 熔断、Redis 滑窗限流、模型回退与 Token 配额** 隔离故障。

## 项目二：惠巷 — 本地生活服务平台

**惠巷 — 本地生活服务平台**　　　　**核心开发者**　　　　**2025.05 - 2025.10**

- **项目简介：** 基于 **Spring Boot 3** 构建的本地生活点评与优惠券秒杀平台，覆盖商铺查询、达人探店、关注互动、优惠券秒杀与订阅提醒等场景，聚焦 **单体高并发下的削峰、缓存、限流与一致性闭环**。
- **技术栈：** Spring Boot 3、MyBatis-Plus、MySQL、Redis/Redisson、Kafka、Lua、Caffeine、Prometheus、Vue 3
- **职责与技术实现：**
  1. **原子扣减：** 设计 **Redis Lua** 脚本聚合活动校验、库存扣减、一人一单与防超卖逻辑，并记录扣减轨迹支撑后续对账补偿。
  2. **异步削峰：** 引入 **Kafka 异步下单** 削峰，生产发送与消费超时均自动回滚 Redis；配合消费幂等、DLQ 与指数退避重试降低写库压力；抢券压测中链路整体 **QPS 1.2k+、接口 P95 90ms**。
  3. **缓存治理：** 通过 **Caffeine + Redis + 布隆过滤器** 构建三级缓存，以空值缓存防穿透、逻辑过期与双重锁异步重建防击穿雪崩，命中率 **95%+**。
  4. **限流防刷：** 实现 **令牌桶 + 滑动窗口** 双模式限流并前置资格令牌，对超限 IP 与用户实施临时封禁，拦截刷单与高频请求。
  5. **运营闭环：** 基于 **Redisson 分片延迟队列** 实现开抢预通知与订阅用户自动候补发券，结合 Top 买家榜单辅助精细化运营。